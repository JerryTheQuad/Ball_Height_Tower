/* #include <iostream>

double getNumFromUser()
{
  std::cout << "Enter a double number: ";
  double num{};
  std::cin >> num;

  return num;
}

char getSignFromUser()
{
  std::cout << "Enter one of the following: +, -, *, or /: ";
  char sign{};
  std::cin >> sign;

  return sign;
}

int main()
{
  double x {getNumFromUser()};
  double y {getNumFromUser()};
  char r {getSignFromUser()};

  if (r == '+')
    std::cout << x << " + " << y << " = " << x + y;
  else if (r == '-')
    std::cout << x << " - " << y << " = " << x - y;
  else if (r == '*')
    std::cout << x << " * " << y << " = " << x * y;
  else if (r == '/')
    std:: cout << x << " / " << y << " = " << x / y;

  return 0;
} */